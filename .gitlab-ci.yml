image: node:8.0.0


stages:
  #- test
  - build
  # - docs

cache:
  paths:
    - node_modules/

before_script:
  - node -v
  - npm -v
  - npm install

#test:
#  stage: test
#  script:
#    -  xvfb-run -a npm test

build:
  stage: build
  script:
   - npm run build

# docs:
#   stage: docs
#   script:
#    - babel-node docs/dev-run.js
